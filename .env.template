# Server Configuration
LISTEN_HOST=0.0.0.0
LISTEN_PORT=12345
SPA_SERVER_HOST=localhost
SPA_SERVER_PORT=54321

# Security Configuration
# Generate a 32-byte key and encode it in base64
# You can use: python -c "from Crypto.Random import get_random_bytes; import base64; print(base64.b64encode(get_random_bytes(32)).decode())"
RIJNDAEL_KEY=your_base64_encoded_32_byte_key_here 